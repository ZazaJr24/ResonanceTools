<Page x:Class="Steamy.Pages.DepotsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Steamy.Controls"
      ScrollViewer.CanContentScroll="False"
      Background="{DynamicResource AppBackgroundBrush}">
  <Grid Margin="30,26,30,30">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <Grid Margin="0,0,0,20">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock Text="Depots" Style="{StaticResource PageTitleStyle}" />
        <TextBlock Text="Authorized depot metadata and selection overview." Style="{StaticResource PageSubtitleStyle}" Margin="0" />
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
        <ui:Button Content="Select all" Command="{Binding SelectAllCommand}" Margin="0,0,8,0" />
        <ui:Button Content="Refresh" Command="{Binding RefreshCommand}" Appearance="Secondary" />
      </StackPanel>
    </Grid>
    <Border Grid.Row="1" Style="{StaticResource FilterBarStyle}">
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <ui:TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="Search app, depot or name"
                    Width="320"
                    HorizontalAlignment="Left" />
        <TextBlock Grid.Column="1" Text="Only authorized metadata is shown" Style="{StaticResource TertiaryTextStyle}" FontSize="11.5" VerticalAlignment="Center" Margin="14,0,12,0" />
        <controls:FilterFlyout Grid.Column="2" FilterSummary="{Binding FilterSummary}" HasActiveFilters="{Binding HasActiveFilters}">
          <controls:FilterFlyout.FilterContent>
            <TextBlock Text="Use the search field to narrow depots by app, depot or name."
                       Style="{StaticResource MutedTextStyle}"
                       FontSize="11.5"
                       TextWrapping="Wrap" />
          </controls:FilterFlyout.FilterContent>
        </controls:FilterFlyout>
      </Grid>
    </Border>
    <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="14" Padding="4">
      <DataGrid ItemsSource="{Binding FilteredDepots}" SelectedItem="{Binding SelectedDepot}" IsReadOnly="False" CanUserAddRows="False" SelectionMode="Single">
        <DataGrid.Columns>
          <DataGridCheckBoxColumn Header="Selected" Binding="{Binding Selected, UpdateSourceTrigger=PropertyChanged}" Width="78"/>
          <DataGridTextColumn Header="App ID" Binding="{Binding AppId}" Width="82"/>
          <DataGridTextColumn Header="Depot ID" Binding="{Binding DepotId}" Width="92"/>
          <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
          <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="100"/>
          <DataGridTextColumn Header="Branch" Binding="{Binding Branch}" Width="100"/>
          <DataGridTextColumn Header="Manifest" Binding="{Binding ManifestId}" Width="140"/>
          <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
          <DataGridTextColumn Header="Updated" Binding="{Binding Updated}" Width="110"/>
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </Grid>
</Page>
