<Page x:Class="Steamy.Pages.HubPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:Steamy.Models" ScrollViewer.CanContentScroll="False"
      Background="{DynamicResource AppBackgroundBrush}">
  <Page.Resources>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
  </Page.Resources>

  <Grid Margin="30,26,30,30">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Grid Margin="0,0,0,20">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock Text="ZazaHub" Style="{StaticResource PageTitleStyle}" Margin="0"/>
        <TextBlock Text="Download-Repository — nur verfügbare Spiele mit DepotDownloaderMOD."
                   Style="{StaticResource PageSubtitleStyle}" Margin="0,4,0,0"/>
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
        <ui:Button Content="Aktualisieren" Command="{Binding RefreshCommand}" Appearance="Secondary"/>
        <ui:ProgressRing Width="24" Height="24" IsIndeterminate="{Binding IsRefreshing}"
                         Visibility="{Binding IsRefreshing, Converter={StaticResource BooleanToVisibilityConverter}}"
                         Margin="10,0,0,0"/>
      </StackPanel>
    </Grid>

    <!-- Status bar -->
    <Border Grid.Row="1" Style="{StaticResource SurfaceCardStyle}" Padding="14,10" Margin="0,0,0,16">
      <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource MutedTextStyle}" FontSize="12"/>
    </Border>

    <!-- Game Grid -->
    <ScrollViewer Grid.Row="2" Style="{StaticResource PageScrollHostStyle}" Padding="0,0,6,0">
      <ItemsControl ItemsSource="{Binding ZazaHubGames}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Width="186" Height="318" Margin="0,4,12,12"
                    Style="{StaticResource CoverCardStyle}"
                    MouseLeftButtonUp="GameCard_Click"
                    Tag="{Binding}"
                    ToolTip="{Binding Name}">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="256"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Cover Art -->
                <Grid ClipToBounds="True" Background="#10131B">
                  <Image Source="{Binding ArtworkImage}" Stretch="UniformToFill"/>
                  <!-- Fallback glyph when no art loaded -->
                  <TextBlock Text="{Binding CoverGlyph}"
                             Foreground="#303746"
                             FontSize="48"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             Visibility="{Binding IsArtworkFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                  <!-- Bottom gradient -->
                  <Rectangle VerticalAlignment="Bottom" Height="70" Fill="{StaticResource CoverFadeBrush}"/>
                  <!-- Size badge -->
                  <Border Style="{StaticResource CoverBadgeStyle}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="8">
                    <TextBlock Text="{Binding Size}" Style="{StaticResource CoverBadgeTextStyle}"/>
                  </Border>
                </Grid>

                <!-- Info -->
                <StackPanel Grid.Row="1" Margin="11,8,11,0" VerticalAlignment="Center">
                  <TextBlock Text="{Binding Name}"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             FontSize="12" FontWeight="SemiBold"
                             TextTrimming="CharacterEllipsis"/>
                  <TextBlock Text="{Binding DepotSummary}"
                             Foreground="{DynamicResource TextTertiaryBrush}"
                             FontSize="10.5" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</Page>
