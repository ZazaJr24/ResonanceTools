<Page x:Class="Steamy.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Steamy.Controls"
      ScrollViewer.CanContentScroll="False"
      Background="{DynamicResource AppBackgroundBrush}">
  <Grid Margin="30,26,30,30">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <Grid Margin="0,0,0,20">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock Text="Logs" Style="{StaticResource PageTitleStyle}" />
        <TextBlock Text="Application events with automatic secret redaction." Style="{StaticResource PageSubtitleStyle}" Margin="0" />
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
        <ui:Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,8,0" />
        <ui:Button Content="Clear logs" Command="{Binding ClearCommand}" Appearance="Danger" />
      </StackPanel>
    </Grid>
    <Border Grid.Row="1" Style="{StaticResource FilterBarStyle}">
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <ui:TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="Search messages or components"
                    Width="340"
                    HorizontalAlignment="Left" />
        <TextBlock Grid.Column="1" Text="Automatic secret redaction enabled" Style="{StaticResource TertiaryTextStyle}" FontSize="11.5" VerticalAlignment="Center" Margin="14,0,12,0" />
        <controls:FilterFlyout Grid.Column="2" FilterSummary="{Binding FilterSummary}" HasActiveFilters="{Binding HasActiveFilters}">
          <controls:FilterFlyout.FilterContent>
            <StackPanel>
              <TextBlock Text="Log level" Style="{StaticResource PillTextStyle}" />
              <ComboBox ItemsSource="{Binding Levels}"
                        SelectedItem="{Binding SelectedLevel}"
                        Margin="0,5,0,0" />
            </StackPanel>
          </controls:FilterFlyout.FilterContent>
        </controls:FilterFlyout>
      </Grid>
    </Border>
    <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="14" Padding="4">
      <DataGrid ItemsSource="{Binding FilteredLogs}" IsReadOnly="True" CanUserAddRows="False">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Time" Binding="{Binding TimestampLabel}" Width="155"/>
          <DataGridTextColumn Header="Level" Binding="{Binding LevelLabel}" Width="86"/>
          <DataGridTextColumn Header="Component" Binding="{Binding Component}" Width="160"/>
          <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="2*"/>
          <DataGridTextColumn Header="App ID" Binding="{Binding AppIdLabel}" Width="82"/>
          <DataGridTextColumn Header="Job ID" Binding="{Binding JobIdLabel}" Width="220"/>
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </Grid>
</Page>
